// テストのセットアップファイル
// 共通のモック設定やグローバル設定をここに記述

import { beforeAll, afterAll, afterEach } from "vitest";

beforeAll(() => {
  // 全テスト実行前の処理
});

afterAll(() => {
  // 全テスト実行後の処理
});

afterEach(() => {
  // 各テスト実行後の処理
});
